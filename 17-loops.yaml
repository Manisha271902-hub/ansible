- name : loop concepts
  hosts: local
  connection : local
  become: yes
  tasks:
  - name : install packages
    ansible.builtin.package:
      name: "{{item}} "
      state: latest

    loop:
      - { name: "nginx", state: "latest"}
      - { name: "mysql",  state: "absent"}
      
      
    