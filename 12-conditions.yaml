- name : Conditons
  hosts: local
  connection: local
  tasks: 
  - name : test numb less than 10
    vars:
      x : 12
    ansible.builtin.debug:
      msg : "yes {{x}} is less than 10"
    when: x < 10