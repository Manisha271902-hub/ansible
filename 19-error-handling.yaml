- name : user creation 
  hosts: local
  connection: local
  become: yes
  tasks:
  - name : check user exixts or not
    ansible.builtin.command: id roboshop
    register: user_output
    ignore_errors: true

  - name : print the output
    ansible.builtin.debug:
      msg: "USer output {{user_output}}"
      
